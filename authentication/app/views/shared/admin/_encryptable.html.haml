%li{ :class => "clearfix record #{cycle("on", "on-hover")}" }
  %span.title
    %strong Password Encryption
    = encrytable_help_tag
  %span.actions
    = link_to refinery_icon_tag("tick.png"), "#",
      :title => t('devise.encryptable.enabled', :encryptor => actor.class.encryptor_name_helper)

