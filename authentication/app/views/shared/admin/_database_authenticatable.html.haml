%li{ :class => "clearfix record #{cycle("on", "on-hover")}" }
  %span.title
    %strong Account Authentication
    = database_authenticatable_help_tag(actor.class)
  %span.actions
    = link_to refinery_icon_tag("tick.png"), "#",
      :title => t('devise.database_authenticatable.working')

