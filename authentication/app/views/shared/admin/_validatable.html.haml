%li{ :class => "clearfix record #{cycle("on", "on-hover")}" }
  %span.title
    %strong Account Validations
    = validatable_help_tag(actor.class)
  %span.actions
    = link_to refinery_icon_tag("tick.png"), "#",
      :title => t('devise.trackable.enabled', :type => actor._type)

